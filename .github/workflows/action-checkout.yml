name: Github actions workflow
on: workflow_dispatch
jobs:
  action-demo:
    runs-on: ubuntu-slim
    steps:
      - name: Checkout the code
        uses: actions/checkout@v4
      - name: Print index.html file content
        run: cat index.html
